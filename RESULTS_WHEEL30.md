# نتائج Wheel-30 مع التجزئة

تاريخ التجربة: **30 أغسطس 2026**.

هذه المرحلة تختبر الفكرة التالية: بدل تمثيل كل الأعداد الفردية داخل المقطع، نمثل فقط الأعداد التي يمكن أن تكون أولية بعد استبعاد مضاعفات `2` و`3` و`5` مسبقًا.

## الفكرة

كل عدد أولي أكبر من `5` يجب أن يكون ضمن أحد البواقي التالية modulo `30`:

```text
1, 7, 11, 13, 17, 19, 23, 29
```

إذن نحتاج فقط إلى:

```text
8 / 30 = 26.666...%
```

من الأعداد الصحيحة، بدل تمثيل:

```text
1 / 2 = 50%
```

من الأعداد في النسخة المجزأة التي تمثل جميع الأعداد الفردية.

بالنسبة إلى فضاء المرشحين الفردي نفسه، يكون الخفض النظري النسبي:

```text
1 - (8/30) / (1/2) = 46.666...%
```

## التطبيق

الملف:

```text
wheel30_segmented_method.py
```

يجمع بين:

- Wheel-30.
- تمثيل بتّي: بت واحد لكل مرشح ممثل.
- Segmentation: مقطع ثابت السعة.
- Streaming: لا تُحفظ قائمة النتائج كاملة إلا إذا طُلبت النسخة packed.
- قاعدة البدء من `p²`.
- إبقاء العدد الأولي نفسه وعدم شطبه.

الأعداد `2, 3, 5` تُخرج صراحة، ثم يبدأ فضاء Wheel-30 من `7`.

يوفر التطبيق:

```python
iter_primes_wheel30_segmented(...)
consume_primes_wheel30_segmented(...)
primes_wheel30_segmented_packed(...)
```

## كيف يجري الشطب؟

عند معالجة عدد أولي `p >= 7` لا نمر على كل مضاعف لـ`p`.

نكتب المركب بالشكل:

```text
m = p * k
```

ولكي يكون `m` ممثلًا داخل Wheel-30، يجب أن يكون `k` نفسه غير قابل للقسمة على `2` أو `3` أو `5`.

لذلك يمر التطبيق على قيم `k` الموجودة في Wheel-30 فقط. وهذا يقلل عدد محاولات الشطب، لا عدد المرشحين المخزنين فقط.

## التحقق من الصحة

أضيفت Wheel-30 إلى:

- `test_primes.py`
- `ci_verify.py`

وشملت الاختبارات:

- القيم المعروفة حتى `100`.
- الحالات الحدية الصغيرة.
- التطابق مع جميع الطرق لكل حد من `0` إلى `500`.
- مقاطع صغيرة لاختبار العبور المتكرر بين حدود المقاطع.
- تحقق مستقل حتى `N = 4999`.

نجح GitHub Actions على Python `3.11` و`3.12` و`3.13`.

## منهج المقارنة

أضيف:

```text
benchmark_wheel30.py
```

وهو يقارن:

1. التجزئة السابقة التي تمثل كل الأعداد الفردية.
2. التجزئة الجديدة Wheel-30.

كلا الطرفين:

- يعمل streaming.
- لا يحتفظ بقائمة ناتج كاملة.
- يستخدم **نفس الحد الأقصى لعدد البتات داخل المقطع**.

في التجربة الرسمية:

```text
segment_units = 32768 bits
```

أي أن bitset المقطع الأقصى في الحالتين هو نظريًا:

```text
32768 / 8 = 4096 bytes
```

الفرق هو أن 32768 بتًا في Wheel-30 تغطي مجالًا عدديًا أكبر، لأنها لا تهدر مواقع على مضاعفات `3` أو `5`.

## التشغيل الرسمي

GitHub Actions run:

```text
33339324099
```

commit:

```text
5a52a8be51976b08c51642a0e633749bbd927b5e
```

البيئة:

- Python **3.12.14**
- Linux / X64
- AMD EPYC 9V74
- 4 CPUs متاحة للـrunner
- `segment_units = 32768`
- تكرار واحد لكل نقطة في هذا التشغيل الاستكشافي

> لأن القياس استخدم تكرارًا واحدًا، أرقام الزمن مناسبة لإظهار الاتجاه والمقارنة الأولية، وليست تقديرًا نهائيًا عالي الدقة للأداء.

## النتائج

| N | عدد الأوليات | odd segmented time | Wheel-30 time | تسريع Wheel-30 | odd peak | Wheel-30 peak |
|---:|---:|---:|---:|---:|---:|---:|
| 100,000 | 9,592 | 0.669818 s | **0.320211 s** | **2.09×** | 8,298 B | **5,483 B** |
| 1,000,000 | 78,498 | 7.107354 s | **3.732740 s** | **1.90×** | **10,704 B** | 10,940 B |
| 10,000,000 | 664,579 | 75.106831 s | **41.355309 s** | **1.82×** | **11,876 B** | 12,148 B |

## عدد المرشحين الممثلين

| N | كل الأعداد الفردية | Wheel-30 | الخفض |
|---:|---:|---:|---:|
| 100,000 | 49,999 | 26,665 | **46.67%** |
| 1,000,000 | 499,999 | 266,665 | **46.67%** |
| 10,000,000 | 4,999,999 | 2,666,665 | **46.67%** |

هذا يطابق النسبة الرياضية المتوقعة.

## محاولات الشطب

| N | odd segmented | Wheel-30 | الخفض التقريبي |
|---:|---:|---:|---:|
| 100,000 | 71,556 | 23,956 | **66.52%** |
| 1,000,000 | 811,068 | 290,386 | **64.20%** |
| 10,000,000 | 8,925,144 | 3,337,972 | **62.60%** |

الخفض في محاولات الشطب أكبر من خفض عدد المرشحين، لأن Wheel-30 لا تبدأ أصلًا بمعالجة مضاعفات `3` و`5`، كما أن مضاعفات كل `p >= 7` تُولد فقط عندما يكون العامل الآخر `k` موجودًا في العجلة.

## عدد المقاطع

مع نفس سعة `32768` بتًا:

| N | odd segmented | Wheel-30 |
|---:|---:|---:|
| 100,000 | 2 | **1** |
| 1,000,000 | 16 | **9** |
| 10,000,000 | 153 | **82** |

إذن كل مقطع Wheel-30 يغطي مجالًا عدديًا أكبر بنفس حجم bitset تقريبًا.

## ماذا حدث للذاكرة؟

عند المجالات الكبيرة بقيت ذروة الذاكرة متقاربة جدًا:

```text
N = 10,000,000
odd segmented   = 11,876 B
Wheel-30        = 12,148 B
```

وهذا متوقع لأن التجربة أعطت الطريقتين نفس ميزانية البتات للمقطع. Wheel-30 لا تهدف هنا إلى تصغير bitset الأقصى؛ بل إلى **استعمال البتات نفسها لتمثيل مجال أكبر وإجراء عمليات أقل**.

يمكن خفض الذاكرة أكثر إذا خفضنا `segment_units` في Wheel-30 مع الحفاظ على نفس المجال العددي التقريبي الذي كان يغطيه المقطع الفردي القديم.

## الاستنتاج

الفكرة نجحت تجريبيًا في هذا التطبيق:

1. استبعاد مضاعفات `2,3,5` من بنية التمثيل نفسها خفض المرشحين بنحو `46.67%` مقارنة بتمثيل كل الأعداد الفردية.
2. عدد محاولات الشطب انخفض بنحو `63%` إلى `67%` في النقاط المقاسة.
3. عدد المقاطع انخفض بنحو النصف تقريبًا.
4. الزمن تحسن في التشغيل الرسمي الأول بنحو `1.82×` إلى `2.09×`.
5. الذاكرة بقيت شبه ثابتة ومقاربة للنسخة المجزأة السابقة لأن ميزانية البتات للمقطع كانت متساوية عمدًا.

## الخطوة التالية

الفكرة الطبيعية التالية هي **Wheel-210**:

```text
210 = 2 * 3 * 5 * 7
```

بعد إدخال `7` في العجلة يصبح عدد البواقي الصالحة في كل دورة:

```text
phi(210) = 48
```

أي كثافة:

```text
48 / 210 = 22.857...%
```

بدل:

```text
8 / 30 = 26.666...%
```

في Wheel-30.

وهذا يختبر مباشرة الجزء التالي من الفكرة: حذف `1/7` من المرشحين الباقين مسبقًا بدل ترك `7` لمرحلة الشطب.
