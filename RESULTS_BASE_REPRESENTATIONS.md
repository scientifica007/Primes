# دراسة تمثيل الأعداد الأولية عبر قواعد العد

تاريخ الدراسة: **30 أغسطس 2026**.

هذه الدراسة تختبر بصورة تجريبية ما إذا كان تغيير قاعدة تمثيل العدد يكشف بنية إضافية في الأعداد الأولية، أو أنه يعيد صياغة بنية حسابية modular معروفة، وما إذا كان يمكن تحويل أفضل القواعد إلى فائدة خوارزمية حقيقية في الغربلة.

## النطاق

تم تحليل جميع القواعد:

```text
b = 2, 3, ..., 100
```

مع اهتمام خاص بـ:

```text
6, 30, 210, 2310
```

واختبار سلسلة العجلات primorial:

```text
6, 30, 210, 2310, 30030, ...
```

أُجريت الدراسة الأساسية أولًا حتى `N = 10,000,000`، ثم وُسعت إلى:

```text
N = 100,000,000
```

لأن النتائج الأولية كانت قابلة للاختبار ضمن الموارد المتاحة.

التشغيل الرسمي عند `10^8`:

```text
GitHub Actions run: 33340596230
commit: 3691523d5b199301bce01b7ad69b0359b7d72fc0
Python: 3.12.14
NumPy: 2.5.2
Matplotlib: 3.11.1
CPU: AMD EPYC 7763
```

النتيجة المرجعية:

```text
pi(100,000,000) = 5,761,455
last prime        = 99,999,989
```

تم تحليل suffixes على **كل** هذه الأوليات. أما تحليل بنية الأرقام الأعمق فاستعمل عينة متطابقة من:

```text
200,000 prime numbers
200,000 composite numbers
```

## البرامج المضافة

- `base_representation_study.py` — الدراسة الأساسية للقواعد والـsuffix والـentropy والانتقالات ومجاميع الأرقام.
- `base_digit_null_study.py` — خط أساس null لتمييز الإشارة الحقيقية من ضجيج sparse transition matrices.
- `primorial_wheel_segmented_method.py` — غربال مجزأ عام لعجلات primorial.
- `benchmark_primorial_wheels.py` — مقارنة `6,30,210,2310,30030`.
- `benchmark_primorial_focus.py` — اختبار مركّز عند `10^8` للعجلات `210,2310,30030` من دون `tracemalloc` حتى لا يشوه قياس الزمن.
- `BASE_STUDY_100M_KEY_RESULTS.csv` — جدول دائم مختصر للقواعد `2..100`.

الـworkflow الثقيل `.github/workflows/base-representation-study.yml` أصبح يدويًا بعد تثبيت النتائج.

---

# 1. آخر رقم: النتيجة الأساسية modular

آخر رقم في قاعدة `b` هو ببساطة:

```text
n mod b
```

ولذلك، باستثناء الأعداد الأولية التي تقسم `b` نفسها، يجب أن يكون آخر رقم في إحدى الفئات التي تحقق:

```text
gcd(r, b) = 1
```

وعدد هذه الفئات هو:

```text
phi(b)
```

إذن كثافة المرشحين النظرية هي:

```text
phi(b) / b
```

وهذا شرط **ضروري** للأولية، لا شرط كافٍ.

## أفضل القواعد داخل 2..100 كمرشح أولي

| القاعدة | العوامل الأولية المختلفة | phi(b)/b |
|---:|---|---:|
| 30 | 2·3·5 | **0.266666667** |
| 60 | 2·3·5 | **0.266666667** |
| 90 | 2·3·5 | **0.266666667** |
| 42 | 2·3·7 | 0.285714286 |
| 84 | 2·3·7 | 0.285714286 |
| 66 | 2·3·11 | 0.303030303 |
| 78 | 2·3·13 | 0.307692308 |
| 6 | 2·3 | 0.333333333 |

الملاحظة المهمة هي أن:

```text
phi(b)/b = product over p|b of (1 - 1/p)
```

لذلك لا تعطي القوى المكررة لعامل أولي فائدة إضافية في التصفية. لهذا `30` و`60` و`90` متساوية تمامًا من هذه الزاوية. إذا كان الهدف هو أقل دورة تحقق الفائدة نفسها فـ`30` هي الأبسط.

## القواعد الخاصة عند N=10^8

| b | phi(b)/b | المرشحون الفعليون | النسبة إلى تمثيل كل الفرديات |
|---:|---:|---:|---:|
| 6 | 0.333333333 | 33,333,332 | 0.666667 |
| 30 | 0.266666667 | 26,666,665 | 0.533333 |
| 210 | 0.228571429 | 22,857,141 | 0.457143 |
| 2310 | 0.207792208 | 20,779,220 | 0.415584 |

القواعد الأولية نفسها سيئة كتصفية لآخر الرقم:

| b أولي | phi(b)/b |
|---:|---:|
| 7 | 0.857143 |
| 11 | 0.909091 |
| 13 | 0.923077 |

أي أن القواعد المركبة الغنية بالعوامل الصغيرة تقلل فضاء المرشحين بوضوح أكبر.

---

# 2. هل توزيع آخر رقم داخل الفئات المسموحة غير منتظم؟

بعد استبعاد الفئات المستحيلة modular، قسنا Shannon entropy وKL divergence من التوزيع المنتظم على الفئات المسموحة.

عند `N=10^8` كانت الانحرافات صغيرة جدًا.

| القاعدة | الفئات المسموحة | KL من uniform (bits) | normalized entropy |
|---:|---:|---:|---:|
| 6 | 2 | 3.82e-9 | 0.9999999962 |
| 10 | 4 | 5.68e-9 | 0.9999999972 |
| 30 | 8 | 2.21e-8 | 0.9999999926 |
| 210 | 48 | 4.16e-7 | 0.9999999255 |
| 2310 | 480 | 1.08e-5 | 0.9999987822 |

حتى أكبر KL بين القواعد `<=100` كان عند `b=97`:

```text
KL = 1.33e-6 bits
normalized entropy = 0.9999997976
```

أي لا توجد قاعدة في هذا المجال أعطت نقصًا معلوماتيًا ذا قيمة عملية بعد conditioning على الشرط modular.

### أمثلة فعلية

في base 10 عند `N=10^8`:

```text
last digit 7: 1,440,495
last digit 3: 1,440,474
last digit 1: 1,440,298
last digit 9: 1,440,186
```

وفي base 30 تراوحت الفئات الثماني تقريبًا بين:

```text
719,984 و 720,363
```

هذه فروق finite-range صغيرة.

## اختبار التوسع من 10^7 إلى 10^8

زاد عدد الأوليات المستخدمة في suffix analysis من:

```text
664,579 -> 5,761,455
```

أي `8.669×`.

كان **وسيط** نسبة KL الجديدة إلى KL القديمة لآخر رقم عبر القواعد `2..100`:

```text
0.1047
```

بينما مقلوب نسبة حجم العينة:

```text
1 / 8.669 = 0.1153
```

أي أن الانحرافات تقل تقريبًا كضجيج finite-sample بدل أن تستقر كإشارة ثابتة.

مثال base 30:

```text
KL at 10^7 = 2.69e-7
KL at 10^8 = 2.21e-8
```

ومثال base 210:

```text
4.95e-6 -> 4.16e-7
```

هذا يدعم تفسير التوزيع كتوزيع قريب جدًا من التكافؤ بين reduced residue classes، لا كبنية رقمية جديدة.

---

# 3. آخر رقمين وثلاثة أرقام

آخر `k` أرقام في base `b` ليست خاصية مختلفة رياضيًا؛ إنها:

```text
n mod b^k
```

وعدد الفئات المسموحة نظريًا هو:

```text
phi(b^k) = phi(b) * b^(k-1)
```

## نتائج مختارة عند 10^8

| b | k | عدد الفئات | المتوقع لكل فئة | KL bits |
|---:|---:|---:|---:|---:|
| 10 | 1 | 4 | 1,440,363 | 5.68e-9 |
| 10 | 2 | 40 | 144,036 | 3.26e-7 |
| 10 | 3 | 400 | 14,404 | 8.62e-6 |
| 30 | 1 | 8 | 720,182 | 2.21e-8 |
| 30 | 2 | 240 | 24,006 | 4.15e-6 |
| 30 | 3 | 7,200 | 800 | 3.12e-4 |
| 210 | 1 | 48 | 120,030 | 4.16e-7 |
| 210 | 2 | 10,080 | 572 | 4.52e-4 |
| 210 | 3 | 2,116,800 | 2.72 | 0.199 |
| 2310 | 1 | 480 | 12,003 | 1.08e-5 |
| 2310 | 2 | 1,108,800 | 5.20 | 0.091 |
| 2310 | 3 | 2,561,328,000 | 0.00225 | 8.796 |

القيم الكبيرة في الصفوف sparse **لا تعني نمطًا غير عشوائي**؛ عندما يكون المتوقع أقل من عدة مشاهدات لكل فئة تصبح معظم الفئات غير مرصودة طبيعيًا، ويصبح entropy محدودًا بحجم العينة.

في الحالات ذات sampling كافٍ، ازداد التجانس بقوة عند الانتقال من `10^7` إلى `10^8`. مثلًا في base 30 مع ثلاثة أرقام:

```text
KL: 0.003128 -> 0.000312
```

أي نحو عشرة أضعاف أقل.

---

# 4. مقارنة prime/composite داخل الفئات المسموحة

لم نكتف بتوزيع الأوليات؛ حسبنا معدل الأولية داخل كل residue class المسموح بها وقارناه بمعدل الأولية العام بعد شرط `gcd(n,b)=1`.

عند `N=10^8`:

- base 30: أعلى lift `1.000252` وأدناه `0.999726`.
- base 210: أعلى `1.001565` وأدناه `0.998472`.
- base 2310: أعلى `1.010996` وأدناه `0.988169`، مع 480 فئة وأعداد أقل في كل فئة.

وعند رفع المجال من `10^7` إلى `10^8` انخفض coefficient of variation للـlift:

```text
base 30:   0.000611 -> 0.000175
base 210:  0.002620 -> 0.000759
base 2310: 0.012209 -> 0.003877
```

إذن لم نجد residue ثابتة تعطي تحسينًا واضحًا في احتمال الأولية بعد أخذ القيود modular الأساسية في الحسبان.

---

# 5. انتقالات الأرقام داخل العدد

تم بناء transition counts من النوع:

```text
P(a_(i+1) | a_i)
```

لكل قاعدة، ثم قورنت الأوليات بالمركبات باستخدام Jensen-Shannon divergence.

ظهر أولًا signal ملحوظ في transition matrix الكاملة. لكن المصفوفة الكاملة تتضمن الانتقال المتصل بالرقم الأخير، والرقم الأخير مقيد أصلًا بعوامل `b`.

لذلك نفذنا اختبارًا أكثر صرامة:

1. حذف الانتقالات المتصلة بالرقم الأقل أهمية للحصول على **internal transitions**.
2. مقارنة prime/composite.
3. بناء null baseline عبر مقارنة نصف عينة الأوليات بالنصف الآخر، ونصف المركبات بالنصف الآخر.

## النتيجة عند 10^8

لم تظهر أي قاعدة `<=100` ذات **excess internal-transition JS موجب** فوق خط أساس الضجيج.

أفضل القيم نفسها كانت سالبة أو قريبة جدًا من الصفر، مثل:

```text
b=2: -2.44e-7 bits
b=4: -8.48e-7
b=5: -1.47e-6
b=6: -5.34e-6
b=10: -2.29e-5
```

الاستنتاج التجريبي الحالي:

> لا يوجد في هذه الدراسة دليل على أن adjacency بين الأرقام الداخلية لعدد أولي تحمل تمييزًا يتجاوز ما نتوقعه من sampling noise والقيود modular على الطرف الأقل أهمية.

هذا لا يثبت الاستقلال المطلق، لكنه يمنعنا من تفسير raw transition bias كاكتشاف جديد.

---

# 6. مجموع الأرقام و b-1

لكل عدد مكتوب في base `b` توجد الهوية المعروفة:

```text
s_b(n) ≡ n (mod b-1)
```

البرنامج تحقق من هذه الهوية تجريبيًا ولم يسجل أي mismatch في العينة.

بعض أقوى الفروق بين prime/composite في digit-sum ظهرت عند:

```text
b=31, 61, 91
```

وكان excess JS نحو `0.581` bits.

لكن:

```text
31 - 1 = 30 = 2*3*5
61 - 1 = 60 = 2^2*3*5
91 - 1 = 90 = 2*3^2*5
```

إذن هذه الإشارة قوية **لأن مجموع الأرقام يعيد اختبار القابلية للقسمة على 2 و3 و5**.

في عينة المركبات كانت مثلًا النسب ذات الصلة:

```text
divisible by 2: 53.16%
divisible by 3: 35.408%
divisible by 5: 21.2575%
```

بينما لا تقع الأوليات الكبيرة في هذه الفئات.

هذا تمييز حقيقي بين prime/composite، لكنه ليس بنية أولية جديدة؛ إنه divisibility test معروف أعيد ترميزه عبر الأرقام.

مثال آخر:

```text
b=210 => b-1 = 209 = 11*19
```

فتظهر اختبارات 11 و19 عبر digit sum. لكنها لا تضيف شيئًا لا يمكن فعله مباشرة modulo 11 و19.

---

# 7. المجموع المتناوب و b+1

لأن:

```text
b ≡ -1 (mod b+1)
```

فإن المجموع المتناوب للأرقام يحقق:

```text
alternating_digit_sum(n) ≡ n (mod b+1)
```

وتحقق البرنامج من ذلك بدون mismatch في العينة.

أمثلة:

```text
base 10 -> b+1=11
base 29 -> b+1=30=2*3*5
base 30 -> b+1=31
base 210 -> b+1=211
base 2310 -> b+1=2311
```

مرة أخرى، أي تمييز ناتج هنا هو اختبار divisibility modular صريح، وليس شرطًا كافيًا للأولية.

---

# 8. ماذا تقول النتائج عن entropy؟

هناك طريقتان مختلفتان لقراءة entropy ويجب عدم خلطهما:

1. **قبل conditioning**: قاعدة مثل 30 لها كثير من الأرقام الأخيرة المستحيلة، ولذلك entropy أقل. هذه فائدة حقيقية للتصفية لكنها مفسرة بالكامل بـ`gcd(r,30)=1`.
2. **بعد conditioning على الفئات المسموحة**: entropy قريب جدًا من الحد الأقصى، ولا توجد قاعدة `2..100` تعطي predictability إضافية ذات قيمة عملية.

إذن "أقل عشوائية ظاهرية" جاءت أساسًا من إزالة residue classes المستحيلة، لا من ترتيب سري داخل الأعداد الأولية الباقية.

---

# 9. سلسلة primorial كقواعد أو دورات غربلة

إذا كان:

```text
M = p#
```

فإن كثافة المرشحين هي:

```text
phi(M)/M = product_(q<=p) (1 - 1/q)
```

وكل إضافة لعدد أولي جديد `p` تبقي من مرشحي المرحلة السابقة نسبة:

```text
(p-1)/p
```

أي تحذف مسبقًا `1/p` منها. هذا هو بالضبط السلوك الذي لاحظناه سابقًا عند الانتقال من Wheel-30 إلى Wheel-210.

## الكثافة النظرية

| M | phi(M) | phi(M)/M | نسبة المواقع إلى bitset الفردي |
|---:|---:|---:|---:|
| 6 | 2 | 0.333333 | 0.666667 |
| 30 | 8 | 0.266667 | 0.533333 |
| 210 | 48 | 0.228571 | 0.457143 |
| 2310 | 480 | 0.207792 | 0.415584 |
| 30030 | 5,760 | 0.191808 | 0.383616 |
| 510510 | 92,160 | 0.180525 | 0.361051 |
| 9,699,690 | 1,658,880 | 0.171024 | 0.342048 |
| 223,092,870 | 36,495,360 | 0.163588 | 0.327176 |

الفائدة في الكثافة تتحسن ببطء، بينما عدد residues في جدول العجلة ينمو سريعًا جدًا. لذلك لا يمكن اختيار عجلة كبيرة فقط لأنها تملك `phi(M)/M` أصغر.

بالمعنى الرياضي يمكن اعتبار هذه السلسلة "قواعد modular هرمية"، لكن خوارزميًا المصطلح الأدق هو **wheel cycles**. لا توجد ضرورة لتحويل الأعداد فعليًا إلى positional base 2310 أو 30030؛ يكفي ترميز residue classes ودورية خطواتها.

---

# 10. Benchmark عملي للعجلات عند N=10^7

التشغيل الرسمي:

```text
GitHub Actions run: 33340664710
```

استُخدم نفس الحد الأقصى للمقطع:

```text
32768 candidate bits = 4096 bytes
```

لكل العجلات.

| M | candidates | strike attempts | time | tracemalloc peak |
|---:|---:|---:|---:|---:|
| 6 | 3,333,332 | 4,839,069 | 49.539 s | 12,300 B |
| 30 | 2,666,665 | 3,337,972 | 35.697 s | 12,532 B |
| 210 | 2,285,712 | 2,534,604 | 28.348 s | **14,304 B** |
| 2310 | 2,077,920 | 2,115,354 | 26.909 s | 61,228 B |
| 30030 | 1,918,080 | 1,805,280 | **23.358 s** | 863,916 B |

إضافة العوامل حسنت الزمن، لكن كلفة جدول العجلة بدأت تظهر بوضوح في الذاكرة:

- `210 -> 2310`: تحسن زمني `1.054×` فقط، مقابل زيادة peak بنحو `4.28×`.
- `2310 -> 30030`: تحسن `1.152×`، مقابل زيادة peak بنحو `14.1×`.
- `210 -> 30030`: العجلة الأكبر أسرع، لكنها تستخدم نحو `60×` peak memory في هذا implementation.

هذه الزيادة لا تأتي من bitset المقطع؛ فهو بقي `4096 B`. تأتي أساسًا من جدول residues/ranks للعجلة الأكبر.

---

# 11. اختبار 10^8 للعجلات التي بدت مثيرة

لأن Wheel-30030 كانت الأسرع عند `10^7`، اختبرنا `210,2310,30030` عند `10^8` من دون `tracemalloc` حتى لا يشوه قياس الزمن.

التشغيل الرسمي:

```text
GitHub Actions run: 33340984906
```

والطرق الثلاث أعطت بالضبط:

```text
5,761,455 primes
last prime = 99,999,989
checksum = 279,209,790,387,276
```

## النتائج

| M | candidates | strike attempts | segments | time |
|---:|---:|---:|---:|---:|
| 210 | 22,857,141 | 28,560,753 | 698 | 26.360 s |
| 2310 | 20,779,220 | 24,075,401 | 635 | 24.699 s |
| 30030 | 19,180,819 | 20,748,015 | 586 | **22.076 s** |

إذن استمرت أفضلية Wheel-30030 في هذا التطبيق:

```text
30030 vs 210: ~1.194× faster
30030 vs 2310: ~1.119× faster
```

لكن اختبار الذاكرة السابق يوضح أن هذه السرعة تأتي مع جدول عجلة أكبر بكثير.

---

# 12. التفسير الرياضي للأنماط

## أنماط مفسرة بالكامل

الآتي كله modular معروف:

- الأرقام الأخيرة الممكنة: `gcd(last_digit,b)=1`.
- آخر رقمين/ثلاثة: residue classes modulo `b^2`, `b^3`.
- كثافة المرشحين: `phi(b)/b`.
- مساواة القواعد ذات radical نفسه مثل `30,60,90`.
- digit sum modulo `b-1`.
- alternating digit sum modulo `b+1`.
- سلسلة primorial وحذف `1/p` إضافي عند إدخال العامل الأولي `p`.

## ما اختبرناه بحثًا عن شيء أبعد من ذلك

- عدم تكافؤ residue classes بعد conditioning.
- entropy داخل الفئات المسموحة.
- prime-rate lift لكل suffix.
- digit-transition matrices.
- internal transitions بعد إزالة تأثير الرقم الأخير.
- prime/composite JS مع null baseline.
- التوسع من `10^7` إلى `10^8` للأنماط التي بدت قابلة للاختبار.

## النتيجة

حتى `10^8` لم يظهر في هذه المقاييس **نمط رقمي داخل تمثيل العدد نفسه** نستطيع اعتباره بنية جديدة غير مفسرة مباشرة بالحساب modular أو بضجيج العينة.

هذا لا يعني أن جميع الإحصاءات الرقمية للأوليات تافهة. توجد نتائج عميقة في نظرية الأعداد حول الأرقام ومجاميعها، كما توجد biases معروفة في residue patterns بين **الأعداد الأولية المتتالية**. لكن هذه ظاهرة مختلفة عن adjacency بين أرقام العدد الأولي الواحد.

---

# 13. ما الفائدة الحقيقية إذن؟

الفائدة الحقيقية ليست أن base 30 أو base 210 "تكشف" الأوليات. الفائدة هي أنها تعطي **ترميزًا مضغوطًا لفضاء المرشحين**.

بدل فحص كل عدد، نخزن فقط الأعداد التي تجتاز مجموعة من الشروط الضرورية مسبقًا.

أفضل صياغة عملية وجدناها حتى الآن هي:

```text
segmentation
+ bit packing
+ streaming
+ primorial wheel
```

## الاختيار العملي

### إذا كان الهدف توازن السرعة والذاكرة

**Wheel-210** هي نقطة التوازن الأقوى في تجاربنا الحالية:

- تحتفظ بـ`22.857%` فقط من الأعداد.
- peak صغير جدًا في benchmark `10^7` (`14.3 KB`).
- تقلل strike attempts بوضوح.
- الانتقال إلى `2310` يعطي تحسنًا زمنيًا محدودًا مقابل نمو أكبر في جدول العجلة.

### إذا كان الهدف السرعة في Python والذاكرة تحت 1 MB مقبولة

**Wheel-30030** كانت الأسرع من العجلات المختبرة عند `10^7` و`10^8`.

لكن هذا لا يعني أنها أفضل بصورة عامة في C/C++ أو عند تعدد الخيوط؛ بنية residues الأكبر تضغط على cache، وقد تتغير نقطة التوازن مع لغة التنفيذ وحجم cache.

### داخل القواعد <=100

**base/modulus 30** هي أفضل prefilter بسيط؛ `60` و`90` لا تضيفان أي فائدة في كثافة المرشحين لأن radical واحد.

---

# 14. جواب السؤال الرئيسي

## هل تغيير قاعدة العد كشف بنية جديدة فعلًا؟

في الدراسة الحالية: **لا، ليس في الأنماط التي اختبرناها حتى `10^8`.**

معظم البنية القوية التي تظهر عند تغيير القاعدة هي إعادة ترميز لبنية modular معروفة:

```text
last digits <-> modulo b^k
digit sum   <-> modulo b-1
alternating <-> modulo b+1
```

وبعد إزالة هذه الآثار ومقارنة الإشارات بخط أساس null، لم يظهر تمييز داخلي ثابت يستحق اعتباره predictor جديدًا للأولية.

## هل هناك فائدة خوارزمية حقيقية؟

**نعم.** لكنها تأتي من استعمال القاعدة كـ**دورة غربلة / wheel encoding** لا كاختبار أولية جديد.

الاستنتاج العملي الحالي للمشروع:

```text
Default low-memory choice: Wheel-210
Simple small wheel:        Wheel-30
Speed-oriented Python:     Wheel-30030 (if larger wheel table is acceptable)
```

والهيكل العام الأفضل:

```text
segmented + packed + streaming + cache-tuned wheel
```

أي أن أهم ما أعطته تجربة تغيير القواعد ليس قانونًا جديدًا للأعداد الأولية، بل طريقة منهجية لاختيار فضاء مرشحين أصغر، وتنظيمه بصورة دورية مناسبة للذاكرة والـcache.

---

# 15. ما يستحق المتابعة

1. **Consecutive-prime residue transitions** بدل digit transitions داخل العدد الواحد؛ توجد في الأدبيات biases finite-range غير تافهة بين residues للأوليات المتتالية.
2. إعادة تنفيذ Wheel-30/210/2310/30030 بلغة منخفضة المستوى مثل C أو Rust مع جداول steps مضغوطة، لأن Python لا يعطي قياسًا نهائيًا لتكلفة cache.
3. قياس segment size مقابل L1/L2 cache لكل wheel، بدل تثبيت 32768 candidate bits للجميع.
4. استخدام bucket sieve للأعداد الكبيرة جدًا، حيث تصبح قائمة sieving primes نفسها عاملًا مهمًا.
5. اختبار wheel tables مضغوطة بدل `tuple[int]`/rank arrays في Python؛ هذا قد يخفض فجوة الذاكرة لـ30030 كثيرًا.

## مراجع نظرية مرتبطة بالتفسير

- Christian Mauduit & Joël Rivat, *Sur un problème de Gelfond : la somme des chiffres des nombres premiers*, Annals of Mathematics 171 (2010), 1591–1646. يثبت نتائج equidistribution عميقة لمجموع أرقام الأوليات.
- Robert J. Lemke Oliver & Kannan Soundararajan, *Unexpected biases in the distribution of consecutive primes*, PNAS 113 (2016). يدرس biases بين residue classes للأوليات **المتتالية**، وهي ظاهرة مختلفة عن digit adjacency داخل عدد واحد.
- `primesieve` algorithm documentation: segmented sieve + wheel factorization، مع اختيار segment وفق cache؛ وتذكر الوثائق أن modulo 30 أو 210 غالبًا نقطة عملية جيدة لأسباب cache.
