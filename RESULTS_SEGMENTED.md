# التجزئة: جعل ذاكرة فضاء البحث شبه ثابتة

تاريخ التجربة: **30 أغسطس 2026**.

تأتي هذه المرحلة بعد ضغط فضاء المرشحين إلى بت واحد لكل عدد فردي، ثم ضغط الناتج بواسطة `array` أو إخراجه بطريقة streaming. بقيت المشكلة التالية: حتى في streaming الكامل كنا نخصص بتًا لكل عدد فردي من `3` إلى `N`، ولذلك كانت الذاكرة ما تزال تنمو خطيًا مع `N`.

الحل المختبر هنا هو **التجزئة Segmentation**.

## الفكرة

بدل تمثيل جميع الأعداد الفردية حتى `N` دفعة واحدة، نقسم المجال إلى مقاطع ثابتة السعة.

السعة الافتراضية في التجربة:

```text
32768 عددًا فرديًا لكل مقطع
```

ولأن كل مرشح يأخذ بتًا واحدًا فقط، فإن bitset المخصص للمقطع يحتاج في أقصى حالة إلى:

```text
32768 / 8 = 4096 bytes
```

في كل مقطع:

1. نبني bitset للمقطع الحالي فقط.
2. نستخدم الأعداد الأولية الأساسية حتى `sqrt(N)` لشطب المركبات.
3. يبدأ شطب العدد الأولي `p` من أكبر القيمتين `p²` وأول مضاعف له داخل المقطع.
4. لا نمثل المضاعفات الزوجية أصلًا؛ لذلك تكون خطوة الشطب `2p`.
5. نُخرج الأعداد الباقية بطريقة streaming.
6. نتخلص من المقطع وننتقل إلى المقطع التالي.

التطبيق موجود في:

```text
segmented_method.py
```

ويوفر:

- `iter_primes_segmented()` — streaming دون حاوية ناتج.
- `consume_primes_segmented()` — استهلاك streaming مع إحصاءات.
- `primes_segmented_packed()` — نفس التجزئة مع حفظ الناتج في `array` مضغوطة عند الحاجة إلى القائمة كاملة.

## ملاحظة مهمة عن تعقيد الذاكرة

الذاكرة ليست ثابتة رياضيًا بالكامل بالنسبة إلى `N` في التطبيق الحالي، لأننا ما زلنا نبني الأعداد الأولية الأساسية حتى:

```text
sqrt(N)
```

لذلك يمكن وصفها بدقة بأنها **شبه ثابتة بالنسبة إلى فضاء البحث الرئيسي**:

- ذاكرة المقطع ثابتة حسب `segment_odd_count`.
- الزيادة المتبقية تأتي من أوليات `sqrt(N)` والـbitset الصغير المستخدم لبنائها.

وهذا أفضل بكثير من تخصيص bitset حتى `N` نفسه.

## التحقق من الصحة

أضيفت الطريقة المجزأة إلى:

- `test_primes.py`
- `ci_verify.py`

ونجحت اختبارات GitHub Actions على Python:

```text
3.11
3.12
3.13
```

كما نجح التحقق المستقل حتى:

```text
N = 4999
```

ويختبر `test_primes.py` أيضًا التجزئة بمقاطع صغيرة جدًا حتى يمر الحساب عبر حدود مقاطع كثيرة، وليس فقط عبر مقطع واحد كبير.

## Benchmark مخصص للتجزئة

أضيف:

```text
benchmark_segmented.py
```

وهو يقارن بين حالتين streaming فقط، حتى لا تؤثر قائمة الناتج في قياس الذاكرة:

1. **full stream**: bitset واحد لكل الأعداد الفردية حتى `N`.
2. **segmented stream**: bitset ثابت السعة لكل مقطع.

يتم التحقق من التكافؤ بمقارنة:

- عدد الأوليات.
- مجموع الأوليات `checksum`.
- آخر عدد أولي.

## التشغيل الرسمي الأول

GitHub Actions run:

```text
33337897297
```

commit:

```text
c037a1f25c0a7def18377c72f0caddf299f3af5d
```

البيئة:

- Python **3.12.14**
- Linux / X64
- AMD EPYC 7763
- 4 CPUs متاحة للـrunner
- `segment_odds = 32768`
- 5 تكرارات وأخذ الوسيط

### النتائج

| N | عدد الأوليات | full stream peak | segmented peak | نسبة ذاكرة full / segmented | full time | segmented time |
|---:|---:|---:|---:|---:|---:|---:|
| 100,000 | 9,592 | 7,787 B | 8,506 B | 0.92× | 0.390237 s | 0.680508 s |
| 1,000,000 | 78,498 | 64,029 B | **10,872 B** | **5.89×** | 4.288280 s | 7.340849 s |

عند `N = 100000` لا تعطي التجزئة فائدة في الذاكرة، لأن bitset الكامل نفسه صغير جدًا (`6250` بايت نظريًا) بينما تضيف التجزئة كلفة أوليات الأساس وكائنات Python.

أما عند `N = 1000000` فانخفضت الذروة من:

```text
64,029 B
```

إلى:

```text
10,872 B
```

أي خفض يقارب:

```text
83.02%
```

## نقطة توسع عند عشرة ملايين

أُجري تشغيل مستقل عند:

```text
N = 10,000,000
```

GitHub Actions run:

```text
33337998971
```

commit:

```text
7fffed722623cca68062692a4b45e4a02b7b3bc5
```

استُخدم تكرار واحد لأن الهدف هنا هو فحص **اتجاه الذاكرة** عند مجال أكبر، وليس إنتاج تقدير زمني عالي الدقة.

النتيجة:

| N | عدد الأوليات | آخر أولي | full stream peak | segmented peak | نسبة ذاكرة full / segmented |
|---:|---:|---:|---:|---:|---:|
| 10,000,000 | 664,579 | 9,999,991 | 626,625 B | **12,116 B** | **51.72×** |

أي أن خفض الذاكرة وصل إلى نحو:

```text
98.07%
```

مقارنة بالـstream الكامل.

## كيف تغيرت الذاكرة مع زيادة N مئة مرة؟

من `100000` إلى `10000000` زاد `N` بمقدار:

```text
100×
```

لكن ذروة segmented streaming تحركت فقط من:

```text
8,506 B
```

إلى:

```text
12,116 B
```

أي زيادة تقارب:

```text
42.44%
```

في المقابل ارتفعت ذروة full streaming من:

```text
7,787 B
```

إلى:

```text
626,625 B
```

أي أكثر من:

```text
80×
```

وهذا هو الدليل التجريبي الأساسي على أن التجزئة أزالت الاعتماد الخطي المباشر لذاكرة فضاء العمل على `N`.

## لماذا لا تبقى الذاكرة عند 4096 بايت فقط؟

لأن `4096 B` هو bitset المقطع فقط. هناك أيضًا:

- كائنات Python والـgenerator.
- `bytearray` الخاصة بأوليات الأساس حتى `sqrt(N)`.
- `array` مضغوطة للأعداد الأولية الأساسية.

القيم النظرية الأساسية تقريبًا:

| N | sqrt(N) | أوليات الأساس | bitset الأساس | array الأساس عند 4 B/عدد | bitset المقطع |
|---:|---:|---:|---:|---:|---:|
| 100,000 | 316 | 65 | 20 B | 260 B | 4,096 B |
| 1,000,000 | 1,000 | 168 | 63 B | 672 B | 4,096 B |
| 10,000,000 | 3,162 | 446 | 198 B | 1,784 B | 4,096 B |

لهذا ترتفع الذروة ببطء مع `N` بدل أن تبقى ثابتة حرفيًا.

## كلفة الزمن

في التطبيق الحالي المكتوب بـPython، التجزئة أبطأ من full streaming بحوالي `1.7×` في النقاط المقاسة.

عند `N = 10000000`:

```text
full stream = 46.066240 s
segmented   = 78.815504 s
```

هذه مقايضة واضحة:

> التجزئة تقلل الذاكرة بقوة مقابل زيادة العمل الإداري وعدد عمليات تخصيص المقاطع والحساب داخل Python.

الهدف الحالي من هذه النسخة هو **التحكم في الذاكرة والتوسع إلى مجالات أكبر**، وليس أن تكون أسرع نسخة زمنية.

## عدد المقاطع

مع `segment_odds = 32768`:

| N | الأعداد الفردية الممثلة إجمالًا | عدد المقاطع |
|---:|---:|---:|
| 100,000 | 49,999 | 2 |
| 1,000,000 | 499,999 | 16 |
| 10,000,000 | 4,999,999 | 153 |

بغض النظر عن عدد المقاطع، لا يوجد أكثر من bitset مقطع واحد حي في فضاء العمل في اللحظة نفسها.

## GitHub Actions

يوجد workflow مستقل:

```text
.github/workflows/segmented-benchmark.yml
```

وهو **يدوي** (`workflow_dispatch`) بعد انتهاء التشغيلين المرجعيين. ويمكن اختيار:

- حدود `N`.
- عدد التكرارات.
- عدد المرشحين الفرديين في كل مقطع.

ويحفظ:

```text
segmented-results.csv
segmented-output.txt
segmented-environment.txt
```

كـartifact لمدة 90 يومًا.

## الاستنتاج الحالي

نجحت التجزئة في حل المشكلة التي بقيت بعد ضغط المرشحين والناتج:

```text
full bitset memory ~ O(N)
```

أصبحت عمليًا:

```text
segment memory = O(S)
+ base-prime memory المرتبطة بـ sqrt(N)
```

حيث `S` هو حجم المقطع الثابت.

في هذه التجربة، زيادة حد البحث من `100000` إلى `10000000` بمقدار `100×` رفعت ذروة الذاكرة المجزأة من نحو `8.5 KB` إلى نحو `12.1 KB` فقط.

العنصر الذي سيصبح عنق الزجاجة التالي عند المجالات الهائلة هو **توليد وتخزين أوليات الأساس حتى sqrt(N)**. ويمكن معالجة ذلك لاحقًا بتجزئة توليد أوليات الأساس نفسها أو باستخدام استراتيجية متعددة المستويات.
