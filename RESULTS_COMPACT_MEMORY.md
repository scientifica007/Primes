# حل مشكلة الذاكرة: تمثيل مضغوط مع إبقاء العدد الأولي

تاريخ التجربة: **30 أغسطس 2026**.

تختبر هذه المرحلة حلًا مباشرًا لمشكلة الذاكرة التي ظهرت في `retain_prime_method.py`، حيث كان استخدام `set` يجعل الفكرة منطقية وواضحة، لكنه مكلف جدًا في الذاكرة.

## 1. الفكرة

نحتفظ بالمبدأ نفسه:

> العدد الأولي يبقى في فضاء العمل، ونحذف المركبات فقط.

لكننا نغير **تمثيل فضاء العمل**.

بدل تخزين كل عدد كعنصر مستقل داخل `set`، نستفيد من حقيقتين:

1. العدد `2` هو العدد الأولي الزوجي الوحيد.
2. بعد التعامل مع `2` لا نحتاج إلى تمثيل أي عدد زوجي آخر.

إذن نمثل فقط:

```text
3, 5, 7, 9, 11, 13, ...
```

ويأخذ كل عدد فردي **بتًا واحدًا فقط**:

```text
1 = ما زال باقيًا
0 = حُذف لأنه مركب
```

التطبيق موجود في:

```text
retain_prime_compact_method.py
```

## 2. علاقة الفهرس بالعدد

إذا كان فهرس البت هو `i`، فإن العدد الذي يمثله هو:

```text
n = 2*i + 3
```

والعكس:

```text
i = (n - 3) // 2
```

وهذا يسمح بالوصول المباشر إلى حالة أي عدد فردي دون تخزين العدد نفسه داخل بنية Hash.

## 3. الحذف

عند معالجة عدد أولي فردي `p`:

- نبقي `p` نفسه.
- نبدأ الحذف من `p²`.
- نتحرك بخطوة `2p` بدل `p` لأن المضاعفات الزوجية غير ممثلة أصلًا.

أي:

```text
p², p² + 2p, p² + 4p, ...
```

مثال `p = 5`:

```text
25, 35, 45, 55, 65, ...
```

لكن ما سبق حذفه يبقى بتّه صفرًا ولا يحتاج إلى تخزين إضافي.

## 4. مقدار التخزين النظري

عند `N = 100000` يوجد `49999` عددًا فرديًا من `3` إلى `99999`.

لذلك تحتاج مصفوفة الحالة إلى:

```text
ceil(49999 / 8) = 6250 bytes
```

أي نحو **6.1 KiB فقط** لتمثيل فضاء المرشحين نفسه.

للمقارنة، النسخة القائمة على `set` تنشئ عشرات الآلاف من كائنات الأعداد ومداخل جدول التجزئة، ولذلك كانت ذروة الذاكرة تقارب عدة ميغابايت.

## 5. التحقق من الصحة

أضيفت الطريقة الجديدة إلى:

- `test_primes.py`
- `ci_verify.py`
- `benchmark.py`

ونجح GitHub Actions على:

```text
Python 3.11
Python 3.12
Python 3.13
```

كما نجح التحقق المستقل حتى:

```text
N = 4999
```

وتطابقت الطريقة الجديدة مع جميع الطرق السابقة ومع المرجع المستقل.

## 6. Benchmark الرسمي على GitHub Actions

- GitHub Actions run: **33337139304**
- commit المقاس: `00f47aa04c4d796fda9a059fce0aa9025668a5dd`
- Python: **3.12.14**
- Runner: **Linux / X64**
- CPU: **AMD EPYC 7763 64-Core Processor**
- CPUs المتاحة للـ runner: **4**
- التكرارات: **5** لكل طريقة ولكل حد
- القيمة المعروضة: **الوسيط Median**

## 7. النتائج

### الزمن

| N | الأصلية | إبقاء الأولي بـ set | إبقاء الأولي المضغوط | المحسنة |
|---:|---:|---:|---:|---:|
| 1,000 | 0.003965 s | 0.002790 s | **0.002324 s** | 0.003002 s |
| 10,000 | 0.057553 s | 0.039301 s | 0.048349 s | **0.039821 s** |
| 100,000 | 0.865600 s | 0.447196 s | 0.582386 s | **0.432870 s** |

### ذروة الذاكرة

| N | الأصلية | إبقاء الأولي بـ set | إبقاء الأولي المضغوط | المحسنة |
|---:|---:|---:|---:|---:|
| 1,000 | 89,980 B | 57,524 B | **5,984 B** | 6,890 B |
| 10,000 | 1,244,332 B | 836,996 B | **49,090 B** | 58,370 B |
| 100,000 | 8,610,372 B | 8,800,704 B | **397,491 B** | 491,206 B |

## 8. النتيجة الأساسية للذاكرة

عند `N = 100000` انخفضت الذروة من:

```text
8,800,704 B
```

في نسخة `set` إلى:

```text
397,491 B
```

في النسخة المضغوطة.

أي خفض بحوالي:

```text
95.48%
```

أو أن نسخة `set` استهلكت نحو:

```text
22.14×
```

ذاكرة أكثر من النسخة المضغوطة في هذا القياس.

والنتيجة اللافتة أن النسخة المضغوطة استخدمت أيضًا ذاكرة أقل من `optimized_method.py`:

```text
397,491 B مقابل 491,206 B
```

أي خفض إضافي يقارب:

```text
19.08%
```

في ذروة الذاكرة المقاسة.

## 9. لماذا الذروة 397 KB رغم أن مصفوفة المرشحين 6.25 KB فقط؟

لأن `tracemalloc` يقيس العملية كاملة، وليس مصفوفة الحالة وحدها.

عند نهاية الحساب تُنشأ قائمة Python تحتوي `9592` عددًا أوليًا. قائمة Python وكائنات `int` الناتجة تستهلك الجزء الأكبر من الذروة.

إذن بعد هذا التحسين لم تعد **مصفوفة المرشحين** هي مشكلة الذاكرة الرئيسة؛ بل أصبح **تمثيل قائمة الناتج نفسها** هو العنصر المهيمن.

وهذا يفتح تحسينًا لاحقًا مستقلًا: تخزين الناتج في بنية مضغوطة مثل `array('I')` أو إرجاع مولد `generator` عند عدم الحاجة إلى قائمة Python كاملة في الذاكرة.

## 10. العمليات الداخلية عند N = 100000

### نسخة set

```text
generated_candidates = 99999
processed_primes = 65
strike_attempts = 193078
newly_removed = 90407
next_candidate_tests = 315
```

### النسخة المضغوطة

```text
represented_odd_candidates = 49999
storage_bytes = 6250
processed_primes = 64
strike_attempts = 71556
newly_removed = 40408
next_candidate_tests = 157
```

العدد `40408` يمثل المركبات الفردية التي حُذفت فعليًا.

أما المركبات الزوجية فلا تدخل فضاء التمثيل أصلًا. وعددها من `4` إلى `100000` هو:

```text
49999
```

وبالتالي:

```text
40408 + 49999 = 90407
```

وهو بالضبط عدد الأعداد المركبة من `2` إلى `100000`.

## 11. المقايضة: الذاكرة مقابل الزمن

حل الذاكرة نجح بوضوح، لكن التنفيذ البتّي الحالي في Python يجري عمليات إزاحة وقناع bitmask لكل وصول، ولذلك كان عند `N = 100000` أبطأ من النسخة المحسنة:

```text
compact   = 0.582386 s
optimized = 0.432870 s
```

أي أن النسخة المضغوطة أبطأ بحوالي **34.5%** في هذا التشغيل.

هذه ليست مشكلة رياضية، بل كلفة تنفيذ bit operations حلقيًا في Python.

## 12. الاستنتاج

مشكلة الذاكرة التي نتجت عن استخدام `set` **حُلّت عمليًا** مع الحفاظ على مبدأ إبقاء العدد الأولي.

التحول كان:

```text
set of Python integers
        ↓
one bit per odd candidate
```

والنتيجة عند `N = 100000`:

```text
8.80 MB  →  0.397 MB
```

مع بقاء الناتج صحيحًا بالكامل.

المرحلة التالية لتحسين الذاكرة أكثر ليست ضغط فضاء المرشحين؛ فقد أصبح صغيرًا جدًا بالفعل. المرحلة التالية هي **ضغط قائمة الأعداد الأولية الناتجة نفسها**، ثم دراسة كيفية استعادة جزء من السرعة المفقودة دون العودة إلى استهلاك الذاكرة العالي.
