# ضغط قائمة الناتج والـStreaming

تاريخ التجربة: **30 أغسطس 2026**.

تأتي هذه المرحلة بعد أن أثبتت تجربة `retain_prime_compact_method.py` أن ضغط فضاء المرشحين نجح، وأن الجزء الأكبر من ذروة الذاكرة أصبح ناتجًا عن إنشاء `list` من كائنات `int` في Python.

## 1. السؤال التجريبي

هل يمكن الحفاظ على المبدأ نفسه:

> العدد الأولي يبقى، والمركبات فقط تُحذف

مع ضغط **فضاء المرشحين** و**قائمة الناتج** معًا؟

تمت تجربة حلين:

1. `array` مضغوطة عندما نحتاج إلى الاحتفاظ بجميع الأعداد الأولية في الذاكرة.
2. `generator` / streaming عندما يمكن استهلاك الأعداد الأولية واحدًا واحدًا ولا نحتاج إلى حاوية ناتج كاملة.

التطبيق موجود في:

```text
retain_prime_packed_output_method.py
```

## 2. تمثيل المرشحين

يبقى كما في النسخة المضغوطة السابقة: بت واحد لكل عدد فردي من `3` إلى `N`.

عند:

```text
N = 100000
```

عدد المرشحين الفرديين هو:

```text
49999
```

وحجم مصفوفة الحالة:

```text
6250 bytes
```

## 3. ضغط الناتج بواسطة array

بدل:

```python
list[int]
```

تستخدم النسخة الجديدة:

```python
array('I')
```

عندما يكفي مجال العدد الصحيح غير الموقع ذي 32 بت، وتنتقل إلى `array('Q')` عند الحاجة إلى مجال أكبر.

في تشغيل GitHub Actions الحالي كان `array('I')` بحجم:

```text
4 bytes لكل عدد أولي
```

وعند `N = 100000` يوجد `9592` عددًا أوليًا، لذلك كان الحجم الخام للناتج:

```text
9592 × 4 = 38368 bytes
```

ومع مصفوفة المرشحين:

```text
38368 + 6250 = 44618 bytes
```

وهو قريب جدًا من ذروة الذاكرة المقاسة فعليًا.

## 4. Streaming

الدالة:

```python
iter_primes_retain_packed(limit)
```

تحتفظ بمصفوفة الحالة المضغوطة، لكنها لا تنشئ قائمة أو `array` لكل الناتج. بعد اكتمال الغربلة تمر على البتات الباقية وتنتج الأعداد الأولية واحدًا واحدًا.

هذا مناسب عندما يكون المستهلك مثلًا:

- ملفًا نكتب إليه النتائج تباعًا.
- قاعدة بيانات.
- حسابًا إحصائيًا لا يحتاج إلى الرجوع إلى جميع الأوليات السابقة.
- pipeline يعالج كل عدد ثم يتخلص منه.

## 5. التحقق من الصحة

أضيفت نسختا `array` وstreaming إلى:

- `test_primes.py`
- `ci_verify.py`
- `benchmark.py`

ونجحت اختبارات GitHub Actions على:

```text
Python 3.11
Python 3.12
Python 3.13
```

كما نجح التحقق المستقل حتى:

```text
N = 4999
```

## 6. Benchmark الرسمي

- GitHub Actions run: **33337494420**
- commit المقاس: `56a8c05cac0f85846dc52546741bec832668ea29`
- Python: **3.12.14**
- Runner OS: **Linux / X64**
- CPU: **AMD EPYC 9V74 80-Core Processor**
- CPUs المتاحة: **4**
- التكرارات: **5** لكل حالة
- القيمة المعروضة: **Median**

## 7. النتائج الزمنية

| N | الأصلية | `set` | بتات + `list` | بتات + `array` | streaming | المحسنة السابقة |
|---:|---:|---:|---:|---:|---:|---:|
| 1,000 | 0.003848 s | 0.002746 s | 0.002292 s | **0.001491 s** | 0.001427 s | 0.002945 s |
| 10,000 | 0.055209 s | 0.038031 s | 0.049848 s | 0.031078 s | **0.030975 s** | 0.039736 s |
| 100,000 | 0.834459 s | 0.449345 s | 0.600430 s | **0.379683 s** | 0.384000 s | 0.434784 s |

ملاحظة: زمن streaming يشمل المرور على كل القيم ومقارنتها تسلسليًا بمرجع خارج فترة تخصيص الذاكرة المقاسة، لذلك يُستخدم أساسًا لتقدير تكلفة الاستهلاك المتدفق، وليس للمقارنة الصرفة مع دالة تعيد حاوية.

## 8. ذروة الذاكرة

| N | الأصلية | `set` | بتات + `list` | بتات + `array` | streaming | المحسنة السابقة |
|---:|---:|---:|---:|---:|---:|---:|
| 1,000 | 89,980 B | 57,524 B | 5,984 B | **2,048 B** | **1,552 B** | 6,890 B |
| 10,000 | 1,244,332 B | 836,996 B | 49,090 B | **6,894 B** | **2,150 B** | 58,370 B |
| 100,000 | 8,610,372 B | 8,800,704 B | 397,491 B | **47,767 B** | **7,767 B** | 491,206 B |

## 9. ماذا حدث عند N = 100000؟

### من `list` المضغوطة إلى `array`

انتقلت الذروة من:

```text
397491 B
```

إلى:

```text
47767 B
```

أي خفض بحوالي:

```text
87.98%
```

وأصبحت `list` تستهلك نحو **8.32×** ذاكرة أكثر من `array` في هذه المقارنة.

### من `set` إلى `array`

انتقلت الذروة من:

```text
8800704 B
```

إلى:

```text
47767 B
```

أي خفض بحوالي:

```text
99.46%
```

أو أن نسخة `set` استهلكت نحو **184×** ذاكرة أكثر.

### مقارنة `array` بالنسخة المحسنة السابقة

```text
47767 B مقابل 491206 B
```

أي أن `array` الجديدة خفضت ذروة الذاكرة بنحو:

```text
90.28%
```

مقارنة بـ `optimized_method.py`، أي قرابة **10.28×** أقل ذاكرة.

### Streaming

الذروة المقاسة كانت:

```text
7767 B
```

وهذا قريب من حجم مصفوفة المرشحين نفسها (`6250 B`) مع مقدار صغير من كلفة Python المؤقتة.

مقارنة بنسخة `set`، الخفض يقارب:

```text
99.91%
```

أي أكثر من **1100×** فرقًا في الذروة المقاسة.

## 10. نتيجة زمنية غير متوقعة ومهمة

ضغط الناتج لم يوفر الذاكرة فقط؛ بل أزال أيضًا كلفة إنشاء آلاف كائنات `int` مستقلة داخل `list`.

عند `N = 100000`:

```text
بتات + array : 0.379683 s
optimized     : 0.434784 s
```

أي أن النسخة ذات الناتج المضغوط كانت أسرع في هذا التشغيل بنحو:

```text
1.15×
```

من النسخة التي كانت الأسرع سابقًا.

كما كانت أسرع من نسخة البتات التي تعيد `list` بنحو:

```text
1.58×
```

وهذا يؤكد أن كلفة تمثيل الناتج كانت تؤثر في الزمن أيضًا، وليس في الذاكرة فقط.

## 11. الاستنتاج

بعد هذه المرحلة أصبح لدينا فصل واضح بين ثلاثة مستويات من استهلاك الذاكرة:

```text
set للأعداد نفسها             -> ميغابايتات
bitset + list[int]             -> مئات الكيلوبايت
bitset + array('I')            -> عشرات الكيلوبايت
bitset + streaming             -> بضعة كيلوبايتات
```

عند `N = 100000` كانت أفضل النتائج الحالية:

```text
أفضل حاوية ناتج كاملة: 47767 B
أفضل استهلاك متدفق:      7767 B
```

وبذلك لم تعد قائمة الناتج نفسها مشكلة ذاكرة عندما نستخدم `array`، ويمكن تقريب استهلاك الذاكرة من حجم فضاء البحث وحده عند استخدام streaming.

## 12. ملاحظة منهجية

قياس streaming في `benchmark.py` يتحقق من كل قيمة مقابل قائمة مرجعية أُنشئت قبل بدء `tracemalloc`. لذلك لا تدخل ذاكرة القائمة المرجعية في ذروة streaming. هذا مقصود لقياس **الذاكرة الإضافية اللازمة للمنتِج المتدفق نفسه**. في استخدام فعلي لا يحتاج إلى مرجع، تكون الفكرة مماثلة: المستهلك يتعامل مع كل عدد ثم يتخلص منه دون الاحتفاظ بجميع النتائج.
